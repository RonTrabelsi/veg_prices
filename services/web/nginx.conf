server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # The analytics can take a few seconds on a cold cache
    location /api/ {
        proxy_pass http://rest_scraper:80/;
        proxy_set_header Host $host;
        proxy_read_timeout 300s;
    }

    # Single page app
    location / {
        try_files $uri $uri/ /index.html;
    }
}
